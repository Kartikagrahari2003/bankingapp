name :  Test Working

on:
  push:
    branches:
      - main

jobs:
  working-job:
    runs-on: ubuntu-latest

    steps:
      - name : Echo Message
        run : echo "Github Action is working sucessfully 😘"